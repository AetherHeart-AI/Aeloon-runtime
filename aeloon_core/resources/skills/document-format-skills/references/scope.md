# 支持范围

- 支持 `.docx` 创建、诊断、标点清理和版式统一。
- WPS 与 Microsoft Office 都通过 OOXML `.docx` 互操作。
- 不接受 `.wps/.dps/.et`；请用户先在 WPS 中另存为 `.docx/.pptx/.xlsx`。
- `official` 预设适合 GB/T 9704 风格公文，`academic` 与 `legal` 适合对应中文材料；用户模板优先于预设。
- 自动排版不能代替事实、法规、单位模板和审批要求核对。
